source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'Transcribe_Example', :exclusive => true do
  pod 'Transcribe', :path => '../'
end

target 'Transcribe_Tests', :exclusive => true do
  pod 'Transcribe', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
